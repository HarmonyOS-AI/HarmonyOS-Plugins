# 原生 Flutter 悬停布局

半开时用折痕 bounds 把可用区域拆为展示区、折痕占位区、操作区。横向折痕通常上下分区，纵向折痕左右分区；方向取自真实 bounds，不用固定比例猜测。

用 `Stack` + 定位或受约束的 `Row`/`Column` 实现。展示和操作复用原业务 Widget 与 Controller，只有父级布局改变。退出悬停后恢复常规断点布局，不新增操作步骤。
